import { cn } from '@/lib/utils'; export function Badge({className,children,variant='default'}:{className?:string;children:React.ReactNode;variant?:'default'|'outline'|'secondary'|'destructive'}){return <span className={cn('inline-flex rounded-full px-2.5 py-1 text-xs font-medium',variant==='default'&&'bg-neutral-100 text-neutral-700',variant==='outline'&&'border bg-white text-neutral-700',variant==='secondary'&&'bg-amber-100 text-amber-800',variant==='destructive'&&'bg-red-100 text-red-800',className)}>{children}</span>}
