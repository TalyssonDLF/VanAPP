import * as React from 'react'; import { cn } from '@/lib/utils'; export const Select=React.forwardRef<HTMLSelectElement,React.SelectHTMLAttributes<HTMLSelectElement>>(({className,...props},ref)=><select ref={ref} className={cn('h-10 w-full rounded-md border border-neutral-300 bg-white px-3 text-sm focus-visible:ring-2 focus-visible:ring-blue-600 disabled:opacity-50',className)} {...props}/>);Select.displayName='Select';
