import{plainToInstance}from'class-transformer';import{validate}from'class-validator';import{CreateVehicleDto}from'../src/vehicles/dto/vehicle.dto';import{documentStatus}from'../src/vehicles/vehicle.constants';describe('vehicles',()=>{it('normaliza placa e RENAVAM válidos',async()=>{const d=plainToInstance(CreateVehicleDto,{plate:'abc-1d23',renavam:'123.456.789-01',brand:'Mercedes-Benz',model:'Sprinter',passengerCapacity:15,type:'VAN'});expect(await validate(d)).toHaveLength(0);expect(d.plate).toBe('ABC1D23');expect(d.renavam).toBe('12345678901')});it('rejeita capacidade e ano absurdos',async()=>{const d=plainToInstance(CreateVehicleDto,{plate:'ABC1D23',brand:'x',model:'',passengerCapacity:0,type:'VAN',modelYear:99999});expect((await validate(d)).length).toBeGreaterThanOrEqual(4)});it('deriva validade de modo determinístico',()=>{const now=new Date('2026-08-17T00:00:00Z');expect(documentStatus(new Date('2026-08-16'),now)).toBe('EXPIRED');expect(documentStatus(new Date('2026-08-30'),now)).toBe('EXPIRING_SOON');expect(documentStatus(new Date('2027-01-01'),now)).toBe('VALID');expect(documentStatus(null,now)).toBe('NO_EXPIRY')})});
