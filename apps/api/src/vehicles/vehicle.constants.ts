export const DOCUMENT_EXPIRING_SOON_DAYS=30;export type DocumentStatus='VALID'|'EXPIRING_SOON'|'EXPIRED'|'NO_EXPIRY';
export function documentStatus(value:Date|null,reference=new Date()):DocumentStatus{if(!value)return'NO_EXPIRY';const today=new Date(Date.UTC(reference.getUTCFullYear(),reference.getUTCMonth(),reference.getUTCDate()));const expiry=new Date(Date.UTC(value.getUTCFullYear(),value.getUTCMonth(),value.getUTCDate()));if(expiry<today)return'EXPIRED';const limit=new Date(today);limit.setUTCDate(limit.getUTCDate()+DOCUMENT_EXPIRING_SOON_DAYS);return expiry<=limit?'EXPIRING_SOON':'VALID'}
